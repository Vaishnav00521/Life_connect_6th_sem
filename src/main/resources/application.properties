# 1. Server Port
server.port=8080

# 2. Supabase PostgreSQL Connection
# CRITICAL: Spring Boot requires the 'jdbc:' prefix before the postgresql:// URL.
spring.datasource.url=${DB_URL:jdbc:postgresql://db.pezuzoaloogaquygiadd.supabase.co:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:[YOUR-PASSWORD]}
spring.datasource.driver-class-name=org.postgresql.Driver

# 3. Hibernate PostgreSQL Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# 4. Email Configuration (Automated Triggers)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:vaishnavshah.005@gmail.com}
# CRITICAL: Use a 16-character "App Password" generated from your Google Account Security settings, NOT your normal Gmail password!
spring.mail.password=${MAIL_PASSWORD:gwpidqbbxtdbsnkh}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true