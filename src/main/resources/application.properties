# 1. Server Port
server.port=8080

# 2. MySQL Connection (The Bridge)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/lifeconnect_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USER:root}
# CRITICAL: Change "password" to your actual MySQL workbench password!
spring.datasource.password=${DB_PASSWORD:Vaish@005210}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 3. Hibernate (The Architect - automatically builds tables)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 4. Email Configuration (Automated Triggers)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:vaishnavshah.005@gmail.com}
# CRITICAL: Use a 16-character "App Password" generated from your Google Account Security settings, NOT your normal Gmail password!
spring.mail.password=${MAIL_PASSWORD:gwpidqbbxtdbsnkh}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true